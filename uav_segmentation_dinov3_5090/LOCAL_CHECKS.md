# 本地验证记录（2026-09-20）

环境：Windows，Python 环境 `D:/Anaconda3/envs/pytorch`，PyTorch 2.5.1，CPU。服务器安装目标为 PyTorch 2.7.1+cu128；两者不能混称为已验证的服务器环境。

已完成：

- 18 项 unittest 全部通过：官方中间层接口、非方形输入、LoRA 梯度与 masked-K 保留、激活检查点、解码器梯度、SAT/Web 结构及归一化、损失、优化器、滑窗预测、ZIP 标签检查、中断续训和下载续传/错误处理。
- 真实 DINOv3 ViT-L/16 SAT 结构 + 默认 width128 解码器，随机初始化、48×64 CPU 输入，前向、损失反向和推理通过。总参数 305,639,469；可训练参数 2,483,245。本次 loss 2.5052，仅证明数值可执行，不是精度结果。
- Python 语法、配置和固定官方源码哈希核验通过。
- setup.sh、run_all.sh 的 Bash 语法检查通过。

未验证：官方授权预训练权重的真实加载、RTX 5090 的显存/吞吐/算子、全量训练、正式提交成绩、完整竞赛提交通知中的所有要求。没有启动付费完整训练。必须在服务器执行 --check-only 后再决定正式训练；不得用本地小输入耗时推断训练时长。

下载测试使用模拟 HTTP 响应，不代表外部服务器可达或授权链接有效。模型 smoke 测试不下载权重。官方代码固定于 reference/dinov3_source.json 所记录的提交。
