# 云端运行检查

run_all.sh 自动运行：Python3.10/Linux/CUDA检查、依赖安装、语法/单元测试、6996张数据哈希与Label.txt检查、500张测试图检查、官方预训练下载/本地哈希记录、全部骨干参数严格加载、真实前后向、至少3次有限梯度更新、4张滑窗验证、峰值显存和时间估算。

必须在真实4090确认：BF16与所有算子、L模型整体前后向、实际峰值显存、30轮+30次双验证+预测是否在预算内。安装和网络时间不在训练估算中。测速失败不会开始正式训练。

GPU基准运行于独立进程，释放测速模型后再正式初始化。保存的断点禁止跨不同代码、配置、划分或已解析precision复用。

使用服务器持久终端/tmux避免SSH断开中止。训练中每100次更新和每轮边界保存；如预测阶段中断，会重做500张预测而不重训。

完整训练后比较两个固定验证集的各类IoU，再上传脚本检查通过的submission.zip取得官方分数。旧官方66分对应实验尚未核实，不将它写入本模型成绩。65.82属于另一个Swin项目。

本地没有连接服务器、购买实例、启动长训练或提交比赛结果。
