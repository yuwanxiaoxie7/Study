# 可复用执行提示词

在 D:/App/Files/uav_segmentation_dino_5090 工程继续优化无人机八类语义分割。原 DINO4090 官方67.40是基线；当前5090候选未经官方评分。目标为租用Linux RTX5090约32GB。原4090工程保留，新工程只维护一套实现，消融通过配置控制。

先读 LITERATURE_10.md 中10篇论文及原始链接：DINOv2、DINOv3、UNetFormer、DPT、ViT-Adapter、SegFormer、Rein++、META、PIDNet、Lovasz-Softmax。明确“直接复现、借鉴思想、暂不采用”，不得拿其他数据集论文成绩预测本比赛涨分。

按以下方案实施并检验：

1. 保留DINOv2-L/14、最后4层QKV LoRA、现有两层SpatialAdapter及RGB多尺度先验。DINOv3仅作后续候选，获得合法权重并验证接口/规则后才考虑替换；不叠加完整SAM或SegFormer第二骨干。
2. 将倒金字塔明确为粗到细空间解码，DINO四层仍是同尺度网格。保留当前逐级3×3融合，不能声称插值产生新细节或已证实优于基线。
3. 借鉴PIDNet边界监督思想，在最终1/4解码特征增加训练专用1通道辅助头。由相邻有效类别变化生成边界标签，忽略0原始标签映射出的未知像素及其邻域，不对裁剪外框制造假边界。均衡计算边缘与非边缘BCE，处理无边缘和全忽略情况。默认边界损失系数0.1；推理不执行辅助头，不改变提交类别。
4. 复用并核对已有Lovasz-Softmax实现，在有效像素、当前存在类别上计算，默认权重0.2。保持原加权CE和0.3Dice。权重为待验证起始值，不冒充论文定值。
5. 默认crop448、width96、batch1×累积8、30轮、固定随机种子和数据划分不变。保留无新增损失、仅边界、仅Lovasz、联合四种配置，同一份代码执行。decoder_only配置仍用于392/64架构对照。
6. 测试边界标签、ignore邻域、全忽略/无边缘退化情况、Lovasz完美/错误预测、辅助头梯度、推理跳过辅助头、完整L模型前后向、续训和ZIP协议。记录哪些检查只是CPU结构检查。
7. 维持一键安装→数据检查→权重检查→GPU测速→训练/续训→验证→预测ZIP；Lovasz增加排序开销，因此必须重新check-only。超过时间/显存限制要明确报告，不能静默降配置。
8. 保留已有提交完整性、SHA256与文件大小输出。不用测试标签调参，不擅自用测试图像做目标域适配。
9. 将10篇选型、实现映射、对照配置、测试结果与服务器运行命令写入文档，重建cloud源码包。不要自动启动付费训练，不承诺超过67.40。

交付可运行代码与上传包，而不仅是建议。若服务器未连接，完成所有本地可验证工作并明确GPU精度、显存与时长仍待服务器实测。
