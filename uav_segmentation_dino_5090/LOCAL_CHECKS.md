# 本地验证

## 10篇论文驱动更新后的最新结果

2026-09-19：14项unittest全部通过；新增直线边界、忽略区域邻域、全忽略/无边界、Lovasz完美/错误预测、辅助头梯度与推理不调用辅助头测试。
完整DINOv2-L随机初始化CPU前后向通过，307,624,653总参数、3,256,013可训练参数；输出[1,8,56,56]，全部可训练参数梯度有限。仅为结构验证，没有训练数据上的精度结论。
38个Python文件语法、5个配置解析、16个上游源码哈希核对通过。云端包由package_cloud.py检查CRC后生成。
下列为此前阶段记录，最新模型参数量与测试数以上述为准。

2026-09-19，Windows CPU测试：9项unittest通过，包括新解码器输出/梯度、LoRA、真实小型ViT提取、ignore、划分、滑窗、ZIP负例及中断续训一致性。
`scripts/smoke_model.py`用于完整DINOv2-L随机初始化的56×56前后向检查；不下载权重，不衡量精度。
`scripts/local_checks.py`做语法和配置检查；`scripts/verify_sources.py`核对vendored官方源码未变。
未做5090 GPU训练、官方预训练权重加载或新版官方评分。CPU测试不证明5090显存和性能。

实际执行完整L模型CPU烟测通过：307,624,556总参数，3,255,916可训练参数；56×56输出形状正确，全部可训练参数有有限梯度，冻结参数无梯度。
36个Python文件语法检查、2个配置解析、16个上游源码哈希检查通过。
数据包Examples.zip内抽查掩码为L模式1024×1024；Label.txt为0忽略、1–8有效类别。
本地4090既有submission.zip通过500张完整校验，7,195,005字节，SHA256=d597ad52f8ca92114152ebcc88e3875c9dd94494c3943830643045f282212bd6。未确认该文件对应哪次官方提交，不能据此认定其官方分数。
本机未发现bash，未运行shell语法检查或Linux安装；这部分必须在服务器验证。
